## Pre-requisites
- Install the Java runtime environment (Nodejs) and package manager(npm)
1. Node js
2. NPM

## Installation
- Install the dependencies using the below command.
`npm i`

## Running the project
`npm start`